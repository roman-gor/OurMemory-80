{
  "rules": {
    "OurMemory": {
      "Veterans": {
        ".read": true,
        ".write": false
      },
      "Burials": {
        ".read": true,
        ".write": false
      },
      "Tours": {
        ".read": true,
        ".write": false
      },
      "Candles": {
        ".read": true,
        "$veteranId": {
          ".write": true,
          ".validate": "newData.isNumber() && newData.val() === (data.exists() ? data.val() : 0) + 1"
        }
      },
      "Submissions": {
        ".read": false,
        "$submissionId": {
          ".write": "auth != null && !data.exists()",
          ".validate": "newData.hasChildren(['id', 'veteranId', 'text', 'contact', 'status', 'createdAt']) && newData.child('id').val() === $submissionId && newData.child('status').val() === 'pending' && newData.child('text').isString() && newData.child('text').val().length <= 5000 && newData.child('createdAt').val() === now"
        }
      }
    }
  }
}
